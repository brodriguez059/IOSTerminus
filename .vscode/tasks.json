{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            "command": "${workspaceFolder}/compile",
            "args": [
                "${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}