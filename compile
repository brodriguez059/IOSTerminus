#!/bin/bash

echo Compilling $1
if [ "$1" = "Terminus" ]; then
    gcc -Iincludes -Wall src/$1.c -o $1
    echo Stored in current directory. It was the terminus shell.
else
    gcc -Iincludes -Wall src/$1.c -o bin/commands/$1
    echo Stored in bin/commands/
fi
